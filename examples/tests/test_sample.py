"""A trivial passing test placeholder. The real point of this example is the
intentionally conflicting requirements-demo.txt, which causes the pip install
step to fail with a dependency resolution error before this test even runs."""

def test_sample():
    assert 1 + 1 == 2
